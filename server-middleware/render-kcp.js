import qs from 'querystring'

export default function(req, res, next) {
  // if (req.method === 'POST' && req.url === '/KcpHtmlLiveEditorMobile') {
  //   // body 객체를 쿼리스트링으로 변환
  //   const queryString = qs.stringify(req.body)
  //   // 원래 URL을 쿼리 포함 URL로 교체
  //   req.url = '/KcpHtmlLiveEditorMobile?' + queryString
  // }
  // next()
}